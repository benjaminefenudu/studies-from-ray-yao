// EVENT EMITTER METHODS & DESCRIPTIONS

// addListener(eventListener)
// Add a listener for an event.

// on(eventListener)
// bind an event to the listener(callback function).

// once(event, listener)
// bind an event to the one-time listener(callback function).

// listenerCount(event)
// Count how many listeners working

// removeListener(event, listener)
// Remove a listener

// removeAllListeners([event])
// Remove all listeners

// setMaxListeners(n)
// Set the maximum number listeners

// emit(event)
// Triggers an event
